---
phase: 01-data-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - src/__init__.py
  - src/data/__init__.py
  - src/preprocess.py
  - tests/conftest.py
  - tests/test_preprocess.py
  - tests/test_schema.py
  - tests/test_leakage_strip.py
  - tests/test_dedup.py
  - tests/test_split_disjoint.py
  - tests/test_build_corpus.py
  - tests/test_leakage_probe.py
  - tests/test_acquire.py
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "pytest runs green on a greenfield repo via `pytest -q`"
    - "preprocess() is idempotent: preprocess(preprocess(x)) == preprocess(x)"
    - "preprocess() preserves URLs, ALL-CAPS, and punctuation"
    - "preprocess() is importable from one module and handles bn/en/code-mixed text"
    - "Raw and processed data, .env, and kaggle.json are gitignored (never committed)"
  artifacts:
    - path: "src/preprocess.py"
      provides: "The single shared preprocess() entry point (DATA-05, D-08)"
      exports: ["preprocess"]
      min_lines: 15
    - path: "tests/conftest.py"
      provides: "Tiny in-repo Bangla+English+code-mixed+URL+CAPS sample fixtures"
      min_lines: 20
    - path: "pyproject.toml"
      provides: "pytest config + slow marker"
      contains: "tool.pytest.ini_options"
    - path: ".gitignore"
      provides: "ignores data/raw, data/processed, .env, kaggle.json, __pycache__"
      contains: "data/raw"
  key_links:
    - from: "tests/test_preprocess.py"
      to: "src/preprocess.py"
      via: "from src.preprocess import preprocess"
      pattern: "from src.preprocess import preprocess"
    - from: "tests/conftest.py"
      to: "tests/*"
      via: "pytest fixtures shared across the suite"
      pattern: "@pytest.fixture"
---

<objective>
Wave 0 foundation: stand up pytest on a greenfield repo, lay down every test stub
the phase needs (so no downstream task ships without an automated verify), gitignore
all data/secret paths, and implement the ONE shared `preprocess()` function (DATA-05,
D-08) — the single cross-phase contract Phases 2/3/7 import unchanged.

Purpose: Nyquist validation requires the test framework + stubs to exist BEFORE any
build task runs, and `preprocess()` is the lossless-normalize contract that the corpus
build (offline) and inference (online) both call. Getting its lossless behavior right
here (preserve URLs/CAPS/punctuation per D-08) unblocks every later wave.
Output: `pyproject.toml`, `.gitignore`, `src/preprocess.py`, `tests/conftest.py`, and
seven red/empty test stubs that downstream plans turn green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates the foundation symbols for the phase:
- `preprocess(text: str) -> str` in `src/preprocess.py` — the shared entry point (DATA-05).
  Imported as `from src.preprocess import preprocess` by build, training (Phase 2/3),
  and inference (Phase 7). Normalizer + whitespace ONLY; idempotent; preserves URLs/CAPS/punct.
- `tests/conftest.py` fixtures: `sample_bn`, `sample_en`, `sample_codemixed`, `sample_url`,
  `sample_caps`, and a tiny labeled `sample_corpus` DataFrame used by downstream split/schema tests.
- Seven test stub files (one per downstream concern) keyed to 01-VALIDATION.md Wave 0 list.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install + configure pytest, gitignore data/secrets, create package skeleton</name>
  <files>pyproject.toml, .gitignore, src/__init__.py, src/data/__init__.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-VALIDATION.md (## Wave 0 Requirements, ## Test Infrastructure)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (## Validation Architecture → Test Framework; ## Security Domain → V14 Config/Secrets)
    - CLAUDE.md (pinned versions; "What NOT to Use")
  </read_first>
  <action>
    Install pytest 7.x (`pip install pytest`). Create `pyproject.toml` with `[tool.pytest.ini_options]`:
    `testpaths = ["tests"]`, `addopts = "-q"`, and register a `slow` marker
    (`markers = ["slow: marks slow integration tests (leakage probe)"]`) so plan 01-07's probe test can be
    deselected on fast runs. Create empty `src/__init__.py` and `src/data/__init__.py` so
    `from src.preprocess import preprocess` resolves as a package. Append to `.gitignore` (currently only
    `.planning/`): `data/raw/`, `data/processed/`, `.env`, `kaggle.json`, `*.kaggle.json`, `__pycache__/`,
    `.pytest_cache/`, `*.parquet`. This satisfies the DATA-01 "raw not committed" guarantee and the V14
    secrets gate (Kaggle token / .env must never be committed) — threat T-01-ID / T-01-SC mitigation. Do NOT
    create `data/raw` or `data/processed` directories with content; only ignore them.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pytest --version && python -c "import tomllib;d=tomllib.load(open('pyproject.toml','rb'));assert 'slow' in str(d['tool']['pytest']['ini_options']['markers'])" && git check-ignore data/raw .env kaggle.json data/processed >/dev/null && echo OK</automated>
  </verify>
  <done>pytest installed; pyproject.toml has pytest config + slow marker; src and src/data are importable packages; git check-ignore confirms data/raw, data/processed, .env, kaggle.json are ignored.</done>
  <acceptance_criteria>
    - `pytest --version` succeeds
    - `git check-ignore data/raw .env kaggle.json data/processed` returns all four (exit 0)
    - `pyproject.toml` registers the `slow` marker
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared preprocess() (DATA-05, D-08) — lossless normalize + whitespace</name>
  <files>src/preprocess.py, tests/conftest.py, tests/test_preprocess.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 1 + "Note on normalize() signature"; Open Question 1 / Assumption A1; Code Examples)
    - CLAUDE.md ("normalizer (csebuetnlp)" MANDATORY git-install; "BanglaBERT without the normalizer" anti-pattern)
  </read_first>
  <behavior>
    - test_idempotent: preprocess(preprocess(x)) == preprocess(x) for bn, en, code-mixed, URL, ALL-CAPS fixtures
    - test_preserves_url_caps_punct: "http" substring survives; "BREAKING!!!" stays "BREAKING!!!" (case + punctuation intact)
    - test_codemixed: a Bangla+English string returns non-empty and is byte-stable across two calls
    - test_none_and_empty: preprocess(None) == "" and preprocess("   ") == ""
    - test_whitespace_collapse: internal runs of whitespace collapse to single spaces; leading/trailing stripped
  </behavior>
  <action>
    Install the csebuetnlp normalizer (`pip install git+https://github.com/csebuetnlp/normalizer`) — git-only and
    MANDATORY (CLAUDE.md). FIRST resolve Assumption A1: read the installed `normalizer` source/signature to learn
    which kwargs `normalize()` exposes and whether defaults strip URLs/case/punctuation. Implement
    `preprocess(text: str) -> str` in `src/preprocess.py` per D-08: call `normalize(text, ...)` with flags that
    PRESERVE URLs, ALL-CAPS, and punctuation (Phase 4 style/malicious signals depend on these), then collapse
    whitespace with a single regex and strip. Handle None/empty → "". Do NOT add dateline/outlet stripping here
    (that is D-09, offline-only, plan 01-04). Keep it pure and idempotent. Populate `tests/conftest.py` with the
    shared fixtures (`sample_bn`, `sample_en`, `sample_codemixed`, `sample_url`, `sample_caps`, plus a tiny labeled
    `sample_corpus` DataFrame with text/label/source_dataset/group_key columns for downstream split + schema tests).
    Write the `test_preprocess.py` assertions in <behavior> against `from src.preprocess import preprocess`. If a
    normalizer default would strip URLs/case/punct and no flag prevents it, document the exact flag chosen in the
    SUMMARY and assert preservation explicitly.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pytest -q tests/test_preprocess.py</automated>
  </verify>
  <done>preprocess() is importable from src.preprocess, idempotent, preserves URLs/ALL-CAPS/punctuation, collapses whitespace, returns "" for None/empty; all tests/test_preprocess.py cases pass; chosen normalizer flags documented (A1 resolved).</done>
  <acceptance_criteria>
    - `pytest -q tests/test_preprocess.py` passes (all 5 behaviors green)
    - `from src.preprocess import preprocess` works from repo root
    - SUMMARY records the resolved normalizer signature/flags (Assumption A1)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Lay down the six downstream test stubs (Nyquist Wave 0 scaffold)</name>
  <files>tests/test_schema.py, tests/test_leakage_strip.py, tests/test_dedup.py, tests/test_split_disjoint.py, tests/test_build_corpus.py, tests/test_leakage_probe.py, tests/test_acquire.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-VALIDATION.md (## Wave 0 Requirements)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (## Validation Architecture → Phase Requirements → Test Map; Concrete assertions list)
  </read_first>
  <action>
    Create one stub file per downstream concern listed in 01-VALIDATION.md and the RESEARCH Test Map. Each stub
    imports its (not-yet-existing) target module inside the test body or behind a `pytest.importorskip(...)` /
    `@pytest.mark.skip(reason="implemented in plan 01-XX")` so the suite is GREEN now (no collection errors) and
    each downstream plan flips its skip to a real assertion. Required test IDs (downstream plans turn these on):
    test_schema.py::test_provenance_complete, ::test_bangla_roundtrip (01-03);
    test_leakage_strip.py::test_no_reuters_dateline (01-04);
    test_dedup.py::test_near_dup_removed (01-05);
    test_split_disjoint.py::test_groups_disjoint, ::test_all_classes_present, ::test_split_ratios (01-05);
    test_build_corpus.py::test_parquet_written, ::test_provenance_schema_on_disk (01-06);
    test_leakage_probe.py::test_source_stripped_near_chance (01-07, mark @pytest.mark.slow);
    test_acquire.py::test_sources_present (01-02, skip if creds absent).
    Stubs must collect cleanly and run green (skipped counts as green). Reference conftest fixtures where useful.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pytest -q && pytest --collect-only -q tests/ | grep -E "test_(schema|leakage_strip|dedup|split_disjoint|build_corpus|leakage_probe|acquire)" >/dev/null && echo OK</automated>
  </verify>
  <done>All seven stub files collect without error; `pytest -q` is green (passed/skipped, zero errors); each required downstream test ID exists as a skipped/stub test ready for its plan to implement.</done>
  <acceptance_criteria>
    - `pytest -q` exits 0 (green; skips allowed)
    - All required test IDs are present in collection (`pytest --collect-only`)
    - No import/collection errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → git | secrets (Kaggle token, .env) and licensed raw data must not cross into the committed repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-ID | Information Disclosure | .gitignore / committed files | mitigate | gitignore `.env`, `kaggle.json`, `data/raw/`, `data/processed/`, `*.parquet`; Task 1 verifies via `git check-ignore`. Honors V14 + DATA-01 "raw not committed" + D-12 |
| T-01-SC | Tampering | npm/pip installs (pytest, normalizer git) | mitigate | normalizer is the documented official git path (CLAUDE.md); pytest is the standard framework; no `[ASSUMED]`/`[SUS]` packages introduced here (legitimacy audit in 01-RESEARCH approved all data deps). No checkpoint needed |
</threat_model>

<verification>
- `pytest -q` green on a fresh checkout (framework live).
- `pytest -q tests/test_preprocess.py` proves the DATA-05 contract (idempotence + URL/CAPS/punct preservation).
- `git check-ignore data/raw data/processed .env kaggle.json` confirms the secret/data ignore gate.
</verification>

<success_criteria>
- pytest installed and configured (`slow` marker registered).
- `src/preprocess.py` exports an idempotent, lossless `preprocess()` importable as `from src.preprocess import preprocess`, preserving URLs/ALL-CAPS/punctuation (D-08, DATA-05).
- Seven downstream test stubs exist and collect green.
- `.gitignore` blocks raw/processed data and secrets.
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-01-SUMMARY.md` when done. Record the resolved
csebuetnlp normalizer signature/flags (Assumption A1) and the measured `pytest -q` runtime
(feeds the latency figure in 01-VALIDATION.md).
</output>
