---
phase: 01-data-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/data/label_map.py
  - src/data/schema.py
  - tests/test_schema.py
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "Every source's native label maps deterministically to one of {real, fake, malicious}"
    - "LIAR collapses per D-06: fake = pants-fire+false+barely-true; real = mostly-true+true; half-true DROPPED"
    - "Every corpus row carries the full provenance schema (text, label, source_dataset, original_label, language, split, dedup_cluster_id) with no nulls"
    - "Parquet round-trips Bangla Unicode byte-identical"
  artifacts:
    - path: "src/data/label_map.py"
      provides: "DATA-02 source_label → 3-class mapping + LIAR collapse (D-06/D-07)"
      exports: ["map_label", "LABELS", "LIAR_COLLAPSE", "map_dataframe"]
      min_lines: 40
    - path: "src/data/schema.py"
      provides: "D-13 provenance schema, pyarrow types, Unicode-safe Parquet read/write"
      exports: ["SCHEMA", "PROVENANCE_COLUMNS", "write_parquet", "read_parquet", "validate_provenance"]
      min_lines: 30
    - path: "tests/test_schema.py"
      provides: "test_provenance_complete + test_bangla_roundtrip (turned on from stub)"
  key_links:
    - from: "src/data/label_map.py"
      to: "{real, fake, malicious}"
      via: "deterministic per-source dict mapping"
      pattern: "real|fake|malicious"
    - from: "src/data/schema.py"
      to: "data/processed/*.parquet"
      via: "pyarrow write_table / read_table with explicit SCHEMA"
      pattern: "pa\\.schema|pq\\.(write|read)_table"
---

<objective>
DATA-02: the unified label mapping (real/fake/malicious) with the locked LIAR collapse (D-06,
half-true DROPPED) and the rich per-sample provenance schema (D-13), persisted as Unicode-safe
Parquet. These are the two contract pieces the build orchestrator (01-06) assembles into the corpus.

Purpose: Define the label semantics and the row schema FIRST (interface-first), so the strip/dedup/
split modules and the orchestrator all build against fixed contracts instead of guessing column shapes.
Output: `src/data/label_map.py`, `src/data/schema.py`, and `tests/test_schema.py` turned green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `map_label(source_dataset, original_label) -> "real"|"fake"|"malicious"` and `map_dataframe(df)` in
  `src/data/label_map.py`. `LABELS = ("real","fake","malicious")`. `LIAR_COLLAPSE` dict encodes D-06.
- `SCHEMA` (pyarrow), `PROVENANCE_COLUMNS` list, `write_parquet(df, path)`, `read_parquet(path)`,
  `validate_provenance(df)` in `src/data/schema.py`. Schema fields per D-13:
  text, label, source_dataset, original_label, language, split, dedup_cluster_id.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement label_map.py — source_label → 3-class with LIAR collapse (D-06/D-07)</name>
  <files>src/data/label_map.py, tests/test_schema.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-06, D-07, locked label schema)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Label Mapping implied by user_constraints D-06/D-07; §Architecture step 1)
    - CLAUDE.md (## Datasets — source label semantics: ISOT real/fake, LIAR 6-way, SMS spam/ham, phishing)
  </read_first>
  <behavior>
    - LIAR "pants-fire"/"false"/"barely-true" → "fake"; "mostly-true"/"true" → "real"; "half-true" → None (DROPPED, D-06)
    - ISOT real→"real", fake→"fake"; BanFakeNews authentic→"real", fake→"fake"
    - SMS Spam "spam"→"malicious", "ham"→ DROPPED (ham is not "real news"); phishing→"malicious"
    - map_dataframe drops rows whose mapping is None (half-true, ham) and asserts all surviving labels ∈ LABELS
    - original_label is always retained on the row (provenance, D-13)
  </behavior>
  <action>
    Implement `src/data/label_map.py` with `LABELS = ("real","fake","malicious")`, a per-source mapping (a dict keyed
    by `source_dataset` → {original_label: 3class|None}), an explicit `LIAR_COLLAPSE` encoding D-06 (fake = pants-fire
    + false + barely-true; real = mostly-true + true; half-true → None DROPPED per D-06; D-07 folds LIAR into the main
    corpus with provenance retained), `map_label(source_dataset, original_label)` returning the 3-class string or None,
    and `map_dataframe(df)` that applies the map, drops None-mapped rows, RETAINS `original_label`, and asserts every
    surviving `label ∈ LABELS`. Malicious is assembled from spam/phishing only (D-01 English-only — document as a
    limitation, do not translate). Write the label-mapping assertions in <behavior> into `tests/test_schema.py`
    (a `TestLabelMap` group) so the collapse and drops are pinned. Use the confirmed source column schemas from the
    01-02 SUMMARY where they affect original_label values.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pytest -q tests/test_schema.py -k "label or LabelMap or collapse"</automated>
  </verify>
  <done>map_label / map_dataframe deterministically map every source's labels to {real,fake,malicious}; LIAR half-true is dropped; SMS ham is dropped; original_label retained; all label-mapping tests pass.</done>
  <acceptance_criteria>
    - `pytest -q tests/test_schema.py -k "label or LabelMap or collapse"` passes
    - half-true maps to None (dropped); pants-fire/false/barely-true → fake; mostly-true/true → real
    - surviving labels are all in LABELS
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement schema.py — D-13 provenance schema + Unicode-safe Parquet I/O</name>
  <files>src/data/schema.py, tests/test_schema.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-12 Parquet, D-13 provenance schema fields)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (## Code Examples → Provenance schema + Parquet round-trip; Concrete assertions list)
    - CLAUDE.md (pyarrow for Unicode Parquet)
  </read_first>
  <behavior>
    - test_provenance_complete: a built DataFrame has columns ⊇ {text,label,source_dataset,original_label,language,split,dedup_cluster_id} with ZERO nulls; validate_provenance raises on missing column or null
    - test_bangla_roundtrip: write a Bangla-bearing row to Parquet, read it back; the Bangla text is byte-identical
    - write_parquet enforces SCHEMA (label ∈ {real,fake,malicious}); read_parquet returns a pandas DataFrame
  </behavior>
  <action>
    Implement `src/data/schema.py` with the pyarrow `SCHEMA` from RESEARCH Code Examples (fields exactly per D-13:
    text str, label str, source_dataset str, original_label str, language str, split str, dedup_cluster_id int64),
    `PROVENANCE_COLUMNS` list, `validate_provenance(df)` (assert column superset + no nulls + label ∈ LABELS, raise
    with a clear message otherwise), `write_parquet(df, path)` (build a `pa.Table.from_pandas(df, schema=SCHEMA)` and
    `pq.write_table`, creating parent dirs), and `read_parquet(path)` (→ pandas). Use pyarrow 24.0.0. Turn on the stub
    tests `test_provenance_complete` and `test_bangla_roundtrip` in `tests/test_schema.py` with the <behavior>
    assertions, using a Bangla fixture from conftest. The round-trip test must assert the read-back Bangla string
    equals the original exactly (Unicode safety, D-12).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pytest -q tests/test_schema.py::test_provenance_complete tests/test_schema.py::test_bangla_roundtrip</automated>
  </verify>
  <done>schema.py defines the full D-13 pyarrow SCHEMA, write_parquet/read_parquet round-trip Bangla byte-identically, validate_provenance enforces completeness + no nulls + valid labels; both named tests pass.</done>
  <acceptance_criteria>
    - `pytest -q tests/test_schema.py::test_provenance_complete tests/test_schema.py::test_bangla_roundtrip` passes
    - SCHEMA has all seven D-13 fields with correct types
    - Bangla round-trips byte-identical
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw dataset content → typed corpus | malformed/mislabeled source rows cross into the unified schema here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-TM | Tampering | label_map / schema validation | mitigate | `map_dataframe` asserts every surviving label ∈ LABELS; `validate_provenance` rejects null/missing columns — bad source rows cannot silently enter the corpus (V5 input validation) |
| T-03-ID | Information Disclosure | provenance rows | accept | provenance retains `original_label` + `source_dataset` only (no PII fields added); raw text handling/redistribution is gated by gitignore (plan 01-01/01-06), not here |
</threat_model>

<verification>
- `pytest -q tests/test_schema.py` green (label mapping + provenance completeness + Bangla round-trip).
- LIAR collapse and SMS-ham drop pinned by tests (D-06).
</verification>

<success_criteria>
- Deterministic source_label → {real,fake,malicious} mapping with LIAR collapse (D-06/D-07) — DATA-02.
- Full D-13 provenance schema with Unicode-safe Parquet I/O and a completeness validator.
- Stub tests `test_provenance_complete` and `test_bangla_roundtrip` are green.
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-03-SUMMARY.md` when done. Record the final per-source
label-mapping table (what each original_label became) and confirm the D-13 schema fields/types as locked
for the orchestrator (plan 01-06).
</output>
