---
phase: 01-data-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/data/leakage_strip.py
  - src/data/language.py
  - tests/test_leakage_strip.py
autonomous: true
requirements: [DATA-03]
must_haves:
  truths:
    - "No Reuters dateline survives in ISOT real bodies after stripping"
    - "Per-source boilerplate (outlet names, bylines, URL-as-source, image credits, ISOT date column) is removed offline, BEFORE splitting"
    - "Stripping lives in leakage_strip.py (offline only) — NOT inside preprocess() (D-09)"
    - "Each row is tagged bn / en / code-mixed / unknown by Bengali-Unicode character ratio"
  artifacts:
    - path: "src/data/leakage_strip.py"
      provides: "DATA-03 per-source offline boilerplate/source-leakage stripping (D-09)"
      exports: ["strip_boilerplate", "strip_isot_dateline", "strip_dataframe"]
      min_lines: 40
    - path: "src/data/language.py"
      provides: "Deterministic bn/en/code-mixed language tag (D-02, D-13)"
      exports: ["detect_language"]
      min_lines: 12
    - path: "tests/test_leakage_strip.py"
      provides: "test_no_reuters_dateline (turned on) + language tagging tests"
  key_links:
    - from: "src/data/leakage_strip.py"
      to: "ISOT real bodies"
      via: "regex strip of '(Reuters) -' dateline"
      pattern: "Reuters"
    - from: "src/data/language.py"
      to: "row.language field"
      via: "Bengali-Unicode ratio"
      pattern: "Bengali|0980"
---

<objective>
DATA-03 (strip half): the offline boilerplate/source-leakage stripping that removes the make-or-break
ISOT Reuters dateline (and analogous outlet/byline/URL/image-credit artifacts) BEFORE splitting (D-09),
plus the deterministic Bengali-ratio language tagger that fills the `language` provenance field (D-02).

Purpose: ISOT's "real" class is 100% Reuters-datelined — a model trained without stripping it hits ~100%
accuracy detecting the string "Reuters" and learns nothing (RESEARCH Pitfall 1). This plan removes the leak;
plan 01-07's probe proves it worked. Language tagging makes per-language macro-F1 reporting possible later.
Output: `src/data/leakage_strip.py`, `src/data/language.py`, `tests/test_leakage_strip.py` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `strip_boilerplate(text, source_dataset) -> str`, `strip_isot_dateline(text) -> str`,
  `strip_dataframe(df) -> df` in `src/data/leakage_strip.py` (offline only — D-09; NOT in preprocess()).
- `detect_language(text) -> "bn"|"en"|"code-mixed"|"unknown"` in `src/data/language.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement leakage_strip.py — per-source boilerplate + the ISOT Reuters dateline (D-09)</name>
  <files>src/data/leakage_strip.py, tests/test_leakage_strip.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Pitfall 1 ISOT Reuters dateline regex; System Architecture step 2; Anti-Patterns "stripping inside preprocess()")
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-09 — stripping is offline, before splitting, NOT in preprocess())
    - CLAUDE.md (## Datasets — ISOT columns title/text/subject/date)
  </read_first>
  <behavior>
    - test_no_reuters_dateline: after strip, re.search for "(Reuters) -" in an ISOT real body is None
    - leading-dateline regex removed from ISOT real bodies; broader "(Reuters)" mentions also handled
    - ISOT date column dropped (years differ systematically between classes — a second leak, Pitfall 1)
    - strip is idempotent and only touches the source it is told to (per-source dispatch, not global)
    - strip_dataframe operates per-row keyed on source_dataset; non-ISOT rows unaffected by the Reuters rule
  </behavior>
  <action>
    Implement `src/data/leakage_strip.py` (OFFLINE only — D-09; must NOT be imported by preprocess()). Provide
    `strip_isot_dateline(text)` using the RESEARCH Pitfall-1 leading-dateline regex (city + "(Reuters) -" prefix)
    plus removal of residual "(Reuters)" mentions; `strip_boilerplate(text, source_dataset)` dispatching per-source
    rules (ISOT dateline; outlet names / bylines / URL-as-source / image-credit patterns where present in BanFakeNews
    and phishing headers); and `strip_dataframe(df)` applying the right rule per `source_dataset` row and DROPPING the
    ISOT date column from the corpus (year leak). Keep rules conservative — strip source artifacts, not legitimate
    content (Pitfall 4 caution). Turn on the stub `test_no_reuters_dateline` and add the <behavior> assertions to
    `tests/test_leakage_strip.py` using a synthetic ISOT-style fixture (e.g. "WASHINGTON (Reuters) - The senate...").
    Do NOT call normalizer here; this is pure regex source-stripping that runs before preprocess() in the build.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pytest -q tests/test_leakage_strip.py -k "reuters or dateline or strip" && python -c "import inspect, src.preprocess as p; assert 'leakage_strip' not in inspect.getsource(p), 'D-09: strip must NOT be in preprocess()'; print('OK')"</automated>
  </verify>
  <done>strip_isot_dateline removes the Reuters dateline; strip_dataframe applies per-source rules and drops the ISOT date column; preprocess() does NOT import the strip module (D-09); test_no_reuters_dateline and strip behaviors pass.</done>
  <acceptance_criteria>
    - `pytest -q tests/test_leakage_strip.py -k "reuters or dateline or strip"` passes
    - No "(Reuters) -" dateline survives in stripped ISOT real bodies
    - `preprocess()` source does not reference leakage_strip (D-09 boundary)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement language.py — Bengali-ratio bn/en/code-mixed tagger (D-02, D-13)</name>
  <files>src/data/language.py, tests/test_leakage_strip.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (## Code Examples → Bengali-ratio language tagging; Assumption A3; "Don't Hand-Roll" — language ID is the one worth hand-rolling)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-02 record per-sample language; D-13 schema language field)
  </read_first>
  <behavior>
    - pure Bangla string → "bn"; pure English string → "en"; mixed Bangla+English → "code-mixed"
    - empty / no-letter string → "unknown"
    - thresholds (mix_lo, mix_hi) are documented module constants; ratio = Bengali letters / (Bengali+Latin letters)
  </behavior>
  <action>
    Implement `src/data/language.py::detect_language(text, mix_lo=0.15, mix_hi=0.85)` per RESEARCH Code Examples:
    count Bengali-block characters (U+0980–U+09FF) vs Latin letters, compute the Bengali ratio, return "bn" if
    ratio ≥ mix_hi, "en" if ratio ≤ mix_lo, "code-mixed" otherwise, and "unknown" when there are no letters. No ML
    model (RESEARCH: ratio is exact for bn vs en and beats fasttext/lingua on short code-mixed text — Assumption A3;
    document the chosen thresholds). Add the <behavior> assertions to `tests/test_leakage_strip.py` (a `TestLanguage`
    group) using conftest bn/en/code-mixed fixtures.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pytest -q tests/test_leakage_strip.py -k "language or Language or codemixed"</automated>
  </verify>
  <done>detect_language returns bn/en/code-mixed/unknown deterministically by Bengali-Unicode ratio with documented thresholds; language tests pass.</done>
  <acceptance_criteria>
    - `pytest -q tests/test_leakage_strip.py -k "language or Language or codemixed"` passes
    - pure-Bangla→bn, pure-English→en, mixed→code-mixed, no-letter→unknown
    - thresholds are documented constants (A3)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw article text → de-leaked corpus | source/publisher artifacts (datelines, outlet names) must be removed before they leak the label into splits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-TM | Tampering (data integrity) | over-/under-stripping | mitigate | conservative per-source regex strips only known artifacts; `test_no_reuters_dateline` proves the critical leak is gone, plan 01-07 probe proves no residual leak survives. Pitfall 4 caution against merging legitimate content |
| T-04-DoS | Denial of Service | regex over malicious text | mitigate | strip operates on dataset text as DATA only (no eval/render); anchored regexes avoid catastrophic backtracking on the offline corpus |
</threat_model>

<verification>
- `pytest -q tests/test_leakage_strip.py` green (dateline removed; language tagging correct).
- D-09 boundary enforced: `preprocess()` does not import the strip module.
- The real leakage gate (probe macro-F1 near chance) is asserted in plan 01-07.
</verification>

<success_criteria>
- ISOT Reuters dateline + ISOT date column removed; per-source boilerplate stripped offline before splitting (DATA-03, D-09).
- Deterministic bn/en/code-mixed/unknown language tagging (D-02).
- Stripping is NOT inside preprocess() (D-09 boundary).
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-04-SUMMARY.md` when done. Record the exact per-source
strip rules applied, the chosen language thresholds (A3), and any boilerplate patterns observed in
BanFakeNews/phishing headers from the 01-02 acquisition (feeds the probe in plan 01-07).
</output>
