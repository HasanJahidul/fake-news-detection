---
phase: 01-data-foundation
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05"]
files_modified:
  - src/data/build_corpus.py
  - tests/test_build_corpus.py
  - reports/corpus_report.md
autonomous: true
requirements: [DATA-02, DATA-04]
must_haves:
  truths:
    - "build_corpus.py runs the full pipeline (load+label → strip → preprocess → language → dedup → group-key → split → write) deterministically with a fixed seed"
    - "It writes data/processed/{train,val,test}.parquet with the complete D-13 provenance schema and no nulls"
    - "It reports class distribution (overall + per split + per language + per source) to reports/corpus_report.md"
    - "Class distribution is reported, NOT physically rebalanced (D-03/D-04 — balancing is train-time, later)"
    - "Everything surviving dedup + leakage removal is kept — no hard size cap (D-05)"
  artifacts:
    - path: "src/data/build_corpus.py"
      provides: "DATA-02/DATA-04 orchestrator — composes 01-03/04/05 modules + preprocess() into the corpus"
      exports: ["build_corpus", "main"]
      min_lines: 70
    - path: "reports/corpus_report.md"
      provides: "Class distribution + language coverage + dedup removal rate (committed; counts only, no text)"
    - path: "tests/test_build_corpus.py"
      provides: "test_parquet_written + test_provenance_schema_on_disk (turned on)"
  key_links:
    - from: "src/data/build_corpus.py"
      to: "data/processed/{train,val,test}.parquet"
      via: "schema.write_parquet after split"
      pattern: "write_parquet|data/processed"
    - from: "src/data/build_corpus.py"
      to: "src.preprocess.preprocess"
      via: "shared preprocess() applied in the build (same fn used at inference)"
      pattern: "from src.preprocess import preprocess"
---

<objective>
DATA-02 + DATA-04 (persistence): the orchestrator that composes every module into the corpus —
load+label-map (01-03) → source-strip (01-04) → shared preprocess() (01-01) → language-tag (01-04) →
dedup (01-05) → group-key+split (01-05) → write Parquet (01-03) — and emits the class-distribution report.

Purpose: This is the integration point that turns the gitignored raw data into the committed-report +
data/processed/*.parquet contract Phases 2/3 consume. It enforces the pipeline ORDER (strip+dedup BEFORE
split) that the leak-free guarantee depends on. Distribution is reported, not rebalanced (D-03/D-04), and
everything surviving dedup + leakage removal is kept with no hard size cap (D-05).
Output: `src/data/build_corpus.py`, `reports/corpus_report.md`, build-integration tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `build_corpus(raw_dir="data/raw", out_dir="data/processed", seed=42)` and `main()` in
  `src/data/build_corpus.py`. CLI: `python -m src.data.build_corpus`.
- Outputs: `data/processed/{train,val,test}.parquet` (gitignored — A5) + `reports/corpus_report.md` (committed).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Implement build_corpus.py orchestrator composing the pipeline in leak-safe order</name>
  <files>src/data/build_corpus.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (## Architecture Patterns → System Architecture Diagram steps 1–8; ## Recommended Project Structure → build_corpus.py; Anti-Patterns "splitting before dedup")
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-03 store natural distribution; D-04 balancing is train-time; D-05 no size cap; D-12 Parquet)
    - 01-03/01-04/01-05 SUMMARYs (exact function signatures + group-key choices to wire)
  </read_first>
  <action>
    Implement `src/data/build_corpus.py::build_corpus(...)` composing the modules in EXACT order (RESEARCH diagram
    steps 1→8): (1) load each raw source + `label_map.map_dataframe` (drops half-true/ham, retains original_label);
    (2) `leakage_strip.strip_dataframe` (offline source-strip + drop ISOT date column); (3) apply
    `src.preprocess.preprocess` to the text column (the SAME shared fn used at inference); (4) `language.detect_language`
    → `language` column; (5) `dedup.dedup_dataframe` → `dedup_cluster_id` (BEFORE splitting — Anti-Pattern guard);
    (6) `split.derive_group_key`; (7) `split.make_splits(seed=42)` → `split` column + `split.assert_disjoint`;
    (8) `schema.write_parquet` each split to `data/processed/{train,val,test}.parquet`. Do NOT physically rebalance
    classes — store the natural distribution (D-03); balancing is train-time (D-04). Keep EVERYTHING that survives
    dedup + leakage removal — no hard row/size cap (D-05). Fixed seed → deterministic, re-runnable (so processed
    Parquet can stay gitignored per A5). Provide `main()` + `python -m src.data.build_corpus`. Validate the assembled
    frame with `schema.validate_provenance` before writing.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -c "s=open('src/data/build_corpus.py').read();[__import__('sys').exit('missing '+x) for x in ['map_dataframe','strip_dataframe','preprocess','detect_language','dedup_dataframe','make_splits','write_parquet'] if x not in s];assert s.index('dedup_dataframe')<s.index('make_splits'),'dedup must precede split';print('OK')"</automated>
  </verify>
  <done>build_corpus composes label_map → strip → preprocess → language → dedup → group-key → split → write in that order (dedup before split); uses the shared preprocess(); does not rebalance and applies no size cap (D-05); writes three Parquet splits; deterministic with seed=42.</done>
  <acceptance_criteria>
    - orchestrator references all module entry points (label_map, strip, preprocess, language, dedup, split, schema)
    - dedup is composed BEFORE split (source-order check)
    - no physical class rebalancing (D-03/D-04) and no hard size cap (D-05)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the build + write reports/corpus_report.md + turn on build-integration tests</name>
  <files>tests/test_build_corpus.py, reports/corpus_report.md</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (## Recommended Project Structure → reports/corpus_report.md; Pitfall 5 commit-processed decision A5; Pattern 4 report contents)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-02 per-language reporting; D-13 schema)
  </read_first>
  <action>
    Run `python -m src.data.build_corpus` on the populated `data/raw/` (from plan 01-02) to produce
    `data/processed/{train,val,test}.parquet`. Generate `reports/corpus_report.md` (COMMITTED — counts only, NO raw
    article text, per SYS-02 + Pitfall 5/A5): overall class distribution; per-split class counts; per-language
    coverage (bn/en/code-mixed/unknown — exposing the malicious-class English-leaning limitation, D-01/D-02); per-source
    row counts; dedup removal rate + mixed-label cluster count; and the deterministic build command. Turn on
    `tests/test_build_corpus.py::{test_parquet_written,test_provenance_schema_on_disk}`: assert the three Parquet files
    exist and read back via `schema.read_parquet` with the full D-13 columns and no nulls; assert each split's labels
    ⊆ {real,fake,malicious}. If creds/raw are absent (CI), skip the on-disk tests and run on a tiny synthetic raw
    fixture so the integration path is still exercised.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pytest -q tests/test_build_corpus.py && test -f reports/corpus_report.md && grep -qiE "class distribution|per-language|language coverage" reports/corpus_report.md && echo OK</automated>
  </verify>
  <done>build runs end-to-end; data/processed/{train,val,test}.parquet exist with the full D-13 schema and no nulls; reports/corpus_report.md (counts only, committed) shows class distribution + per-language + per-source + dedup rate; build-integration tests pass (or skip cleanly without raw data while still exercising the synthetic path).</done>
  <acceptance_criteria>
    - `pytest -q tests/test_build_corpus.py` passes (or skips on-disk parts cleanly, synthetic path still runs)
    - `reports/corpus_report.md` exists with class distribution + per-language coverage
    - on-disk Parquet (when built) has complete D-13 schema, no nulls
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gitignored raw → committed report + processed Parquet | licensed/PII raw text must not leak into the committed report or repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-ID | Information Disclosure | reports/corpus_report.md + processed Parquet | mitigate | report carries COUNTS ONLY, no raw article text (SYS-02); processed Parquet gitignored (A5/Pitfall 5) so CC BY-NC-SA-derived content is not redistributed; deterministic rebuild documented instead |
| T-06-TM | Tampering (pipeline order) | build composition | mitigate | orchestrator hard-orders dedup+strip BEFORE split; `validate_provenance` + `assert_disjoint` fail the build if the leak-free/schema guarantees break |
</threat_model>

<verification>
- `pytest -q tests/test_build_corpus.py` green (Parquet written, schema complete, no nulls).
- `reports/corpus_report.md` shows class distribution + per-language + per-source + dedup rate.
- Pipeline order (dedup before split) statically and behaviorally enforced.
</verification>

<success_criteria>
- Deterministic build writes data/processed/{train,val,test}.parquet with full D-13 provenance (DATA-02).
- Source-disjoint leak-free splits at natural distribution; distribution reported not rebalanced (DATA-04, D-03/D-04); no size cap (D-05).
- Committed corpus report carries counts only (no raw text); processed data gitignored (A5).
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-06-SUMMARY.md` when done. Record final row counts per split/
class/language/source, the dedup removal rate, the deterministic build command, and confirm the
data/processed gitignore decision (A5) — plan 01-07 runs the leakage probe against these splits.
</output>
