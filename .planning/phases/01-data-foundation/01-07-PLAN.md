---
phase: 01-data-foundation
plan: 07
type: execute
wave: 5
depends_on: ["01-06"]
files_modified:
  - src/data/leakage_probe.py
  - tests/test_leakage_probe.py
  - reports/corpus_report.md
autonomous: true
requirements: [DATA-03]
must_haves:
  truths:
    - "A leakage probe runs three degraded views: title-only, single-body-sentence, and source-stripped"
    - "On source-stripped input the probe macro-F1 is meaningfully below the full-content model AND not near-ceiling (< 0.95)"
    - "No probe scores near-ceiling on degraded input (a residual leak would show here)"
    - "Probe macro-F1 + top-20 features per view are written to reports/corpus_report.md"
  artifacts:
    - path: "src/data/leakage_probe.py"
      provides: "DATA-03 leakage probe — TF-IDF+LogReg on title-only / single-sentence / source-stripped views"
      exports: ["run_probe", "probe_all_views", "top_features"]
      min_lines: 50
    - path: "tests/test_leakage_probe.py"
      provides: "test_source_stripped_near_chance (turned on, @pytest.mark.slow)"
    - path: "reports/corpus_report.md"
      provides: "appended probe section: per-view macro-F1 + top-20 features + PASS/FAIL verdict"
  key_links:
    - from: "src/data/leakage_probe.py"
      to: "data/processed/{train,test}.parquet"
      via: "schema.read_parquet → degraded-view feature extraction"
      pattern: "read_parquet|data/processed"
    - from: "src/data/leakage_probe.py"
      to: "macro-F1 verdict"
      via: "sklearn TfidfVectorizer + LogisticRegression + classification metrics"
      pattern: "TfidfVectorizer|f1_score|macro"
---

<objective>
DATA-03 (probe half) — the phase GATE: prove the boilerplate stripping actually killed the leak. Train a
cheap TF-IDF+LogReg on three deliberately-degraded views (title-only, single-body-sentence, source-stripped)
and assert the source-stripped probe can't cheat (macro-F1 well below full-content and not near-ceiling).

Purpose: This is success-criterion #3 — without it, plan 01-04's strip is unverified. Inspecting the top
features is as important as the score: "reuters", a city, an outlet, or a 4-digit year in the top-20 is a
direct leak signal (RESEARCH Pattern 4 / Pitfall 1). Runs on the built splits from 01-06.
Output: `src/data/leakage_probe.py`, `tests/test_leakage_probe.py` (slow) green, probe section appended to report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `run_probe(df_train, df_test, view) -> {macro_f1, top_features}` for view ∈ {full, title, sentence,
  source_stripped}, `probe_all_views(...)`, and `top_features(model, vectorizer, n=20)` in
  `src/data/leakage_probe.py`.
- Appended probe section in `reports/corpus_report.md`: per-view macro-F1, top-20 features, PASS/FAIL.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement leakage_probe.py — three degraded-view TF-IDF+LogReg probes + top-feature inspection</name>
  <files>src/data/leakage_probe.py, tests/test_leakage_probe.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 4 leakage probe — the three views + PASS/FAIL rule; Pitfall 1 warning signs; § Concrete assertions: source_stripped macro-F1 < full AND < 0.95)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (success-criterion #3; D-09 leakage gate)
    - CLAUDE.md (TF-IDF + LogisticRegression; bnlp-toolkit for Bangla sentence split)
  </read_first>
  <behavior>
    - test_source_stripped_near_chance (@pytest.mark.slow): on a fixture where the leak HAS been stripped, source_stripped macro-F1 < full-content macro-F1 AND < 0.95
    - a deliberately-NOT-stripped fixture (Reuters dateline intact) makes the probe score near-ceiling → demonstrates the probe detects leaks (negative control)
    - top_features returns the n most predictive TF-IDF tokens per class for inspection
    - probe uses class-stratified macro-F1 (not accuracy), trained on train split, scored on test split
  </behavior>
  <action>
    Implement `src/data/leakage_probe.py` per RESEARCH Pattern 4. `run_probe(df_train, df_test, view)` builds features
    for one of: `full` (whole body), `title` (title only), `sentence` (one randomly-sampled body sentence — use
    `bnlp-toolkit` for Bangla sentence splitting, a simple splitter for English), `source_stripped` (body AFTER
    leakage_strip). Fit `TfidfVectorizer` + `LogisticRegression(class_weight="balanced")` on train, score macro-F1 on
    test, return macro-F1 + `top_features`. `probe_all_views` runs all four and applies the PASS/FAIL rule: PASS when
    source_stripped macro-F1 is meaningfully below full-content AND no probe is near-ceiling (≥0.95 on degraded input
    = FAIL — a leak survives). Inspect top features for "reuters"/city/outlet/4-digit-year tells. Add the <behavior>
    tests to `tests/test_leakage_probe.py`, marking the real-corpus test `@pytest.mark.slow`; include the negative-control
    fixture (un-stripped → near-ceiling) so the probe's sensitivity is proven. Read splits via `schema.read_parquet`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pytest -q tests/test_leakage_probe.py -m "not slow" && python -c "import ast;s=open('src/data/leakage_probe.py').read();[__import__('sys').exit('missing '+x) for x in ['run_probe','probe_all_views','top_features','TfidfVectorizer'] if x not in s];print('OK')"</automated>
  </verify>
  <done>leakage_probe.py runs full/title/sentence/source_stripped probes with macro-F1 + top features; the PASS/FAIL rule (source_stripped < full AND < 0.95) is implemented; fast tests + negative control pass; slow real-corpus test exists.</done>
  <acceptance_criteria>
    - `pytest -q tests/test_leakage_probe.py -m "not slow"` passes (incl. negative control)
    - run_probe/probe_all_views/top_features present; uses macro-F1 not accuracy
    - PASS/FAIL rule encoded (source_stripped below full AND < 0.95)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the probe on the built corpus + append PASS/FAIL probe section to the report</name>
  <files>reports/corpus_report.md, tests/test_leakage_probe.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 4 "Report" bullet — write each probe's macro-F1 + top-20 features; PASS/FAIL judgement vs informed-chance ≈ 0.33)
    - 01-06-SUMMARY.md (built split paths + counts)
  </read_first>
  <action>
    Run `probe_all_views` on the real `data/processed/{train,test}.parquet` from plan 01-06. Append a "Leakage Probe"
    section to `reports/corpus_report.md`: a table of per-view macro-F1 (full / title / sentence / source_stripped),
    the top-20 features per view, and an explicit PASS/FAIL verdict with one-line rationale (judge relative to 3-class
    informed-chance ≈ 0.33 and to the full-content model; FAIL if any degraded view ≥0.95 or "reuters"/outlet/year in
    top features). Write the verdict line in the form `Leakage probe: PASS` (or `Leakage probe: FAIL`) so it is
    machine-checkable. If FAIL, the SUMMARY must flag that plan 01-04's strip needs another rule and re-run (do not
    paper over it). Enable the `@pytest.mark.slow` real-corpus test `test_source_stripped_near_chance` to run against
    the built splits. Built Parquet is a PRECONDITION of this gate: the slow test MUST error/fail (not skip) when
    `data/processed/*.parquet` is absent — load the splits eagerly and let the missing-file error propagate; do NOT
    wrap it in `pytest.skip` or `importorskip`. The phase cannot close on a skipped probe — this is the leakage gate,
    so the slow test must actually RUN and PASS, and the report must carry a `Leakage probe: PASS` verdict, before
    /gsd-verify-work.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pytest -q tests/test_leakage_probe.py -m slow && grep -qiE "leakage probe[: ].*pass" reports/corpus_report.md && ! grep -qiE "leakage probe[: ].*fail" reports/corpus_report.md && echo OK</automated>
  </verify>
  <done>probe ran on the real splits; reports/corpus_report.md has a Leakage Probe section with per-view macro-F1 + top-20 features + an explicit `Leakage probe: PASS` verdict (and NO FAIL verdict); the slow probe test RAN (not skipped) against the built data/processed/*.parquet and passed; built Parquet is a precondition — the slow test errors/fails if Parquet is absent, never skips green; the phase cannot close on a skipped probe; any FAIL is flagged for a strip fix, not hidden.</done>
  <acceptance_criteria>
    - report contains a Leakage Probe section with per-view macro-F1 + a `Leakage probe: PASS` verdict and no FAIL verdict
    - the slow test RUNS (not skipped) against the built data/processed/*.parquet and passes; it errors/fails when Parquet is absent
    - top-20 features inspected for leak tells (reuters/outlet/year)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built corpus → trust claim | a residual source artifact would let downstream models cheat; the probe is the gate that detects it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-TM | Tampering (undetected leak) | leakage probe validity | mitigate | three degraded views + a negative-control fixture (un-stripped → near-ceiling) prove the probe actually detects leaks; PASS requires source_stripped < full AND < 0.95; the slow gate must RUN (errors if Parquet absent, never skips green) so the phase cannot close on an un-run probe; top-feature inspection catches residual outlet/year tells (Pitfall 1) |
| T-07-ID | Information Disclosure | probe report | accept | report logs macro-F1 + token features (not raw articles); top features are short tokens, no PII/article bodies redistributed (SYS-02) |
</threat_model>

<verification>
- `pytest -q tests/test_leakage_probe.py -m slow` green — the slow real-corpus probe RUNS (not skipped) and passes (phase gate).
- `reports/corpus_report.md` Leakage Probe section shows per-view macro-F1 + a `Leakage probe: PASS` verdict (no FAIL) + top features.
- Negative control proves the probe detects an un-stripped leak (sensitivity check).
</verification>

<success_criteria>
- Title-only / single-sentence / source-stripped probes run; source-stripped macro-F1 below full-content and not near-ceiling (DATA-03, success-criterion #3).
- Top-20 features per view inspected; no leak tells (reuters/outlet/year) — else flagged for a strip fix.
- The slow leakage-gate test runs against the built corpus and passes; the report records a `Leakage probe: PASS` verdict; the phase does not close on a skipped probe.
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-07-SUMMARY.md` when done. Record the per-view macro-F1 table,
the PASS/FAIL verdict, the top features observed, and — if FAIL — exactly which residual artifact leaked and
the follow-up strip rule needed (loops back to plan 01-04). This closes the Phase 1 leakage gate.
</output>
</content>
