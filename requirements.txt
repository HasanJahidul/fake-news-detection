# Core
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6.0
joblib>=1.3
pyarrow>=14.0          # parquet
tqdm>=4.66

# NLP / preprocessing
nltk>=3.8
regex>=2023.0
emoji>=2.8

# Transformer (XLM-R / BanglaBERT) — Colab installs CUDA torch automatically
torch>=2.0
transformers>=4.40
datasets>=2.18
accelerate>=0.28

# URL / article extraction (UI)
trafilatura>=1.8
requests>=2.31
beautifulsoup4>=4.12

# External verification
newsapi-python>=0.2.7

# Plots / report assets
matplotlib>=3.7
seaborn>=0.13

# UI
streamlit>=1.30

# Bangla normalization (optional but recommended)
bnlp-toolkit>=4.0 ; python_version >= "3.8"
