# Runtime dependencies for the data pipeline + classical models (v1.0).
# Install with:  pip install -r requirements.txt
#
# Note: normalizer (csebuetnlp) is git-only and MANDATORY — preprocessing
# (and therefore training) will not work without it.

pandas>=2.2
pyarrow==24.0.0
datasketch==1.10.0
scikit-learn==1.6.1
joblib>=1.4
numpy>=1.26

# Data download from Kaggle (needs a free Kaggle API token — see SETUP.md)
kaggle>=1.6

# Bangla Unicode normalizer — required before any preprocessing
git+https://github.com/csebuetnlp/normalizer

# Phase 3 transformer training/inference stack (CLAUDE.md-pinned).
# Imports cleanly against the existing numpy 2.4.6 / pandas 3.0.3 — NO numpy<2.3
# constraint was required (A1 / Pitfall 7 resolved at install: numpy-2 compatible).
# Do NOT bump to transformers 5.x or torch 2.12 — the project pins the tested 4.46 / 2.6 line.
torch==2.6.*
transformers==4.46.*
accelerate>=0.26
datasets
evaluate
sentencepiece  # required for the XLM-R (sentencepiece) tokenizer — D-04
